version: '3.8'

services:
  toollo:
    container_name: toollo-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '3010:3000'
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
